"""Agent process concerns: the cached Agent instances and the SSE stream.

Empty for now. `get_agent`, `_get_catalog_agent`, `_run_agent_stream` and the
tool-call summarisers still live in `main.py` — roughly 310 lines that contain
no HTTP at all and move here in a later step.
"""
